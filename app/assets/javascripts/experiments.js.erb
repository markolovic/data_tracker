jQuery(document).ready(function($) {
	$('.new').click(function(event) {
		$('.popup').toggle();
	});

	$('.close').click(function(event) {
		$('.popup').hide();
	});

var dayArr = []

function days() {
	
	c = 0

	for (var i = gon.dep_vars[0].length - 1; i >= 0; i--) {
		
		c +=1
		dayArr[i] = "Day" + c
	}
}
function chartist(array) {
	new Chartist.Line('.ct-chart', {
	  labels: array,
	  series: [
	    {
	      name: gon.dep_vars[0].name,
	      data: gon.dep_vars[0]
	    },
	    {
	      name: gon.ind_vars[0].name,
	      data: gon.ind_vars[0]
	    }
	  ]
	});
}


days();
chartist(dayArr)
});

